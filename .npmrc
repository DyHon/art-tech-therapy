# React 19 is newer than some deps' declared peer ranges (e.g. visx, which is
# render-only and React-19-compatible at runtime). Allow installs to proceed despite
# the stale peer metadata so `npm install` works on a fresh clone.
legacy-peer-deps=true
